{
	"private": true,
	"name": "eleanes",
	"description": "the eleanes monorepo, powered by turborepo",
	"packageManager": "pnpm@9.0.0",
	"scripts": {
		"dev": "turbo run dev",
		"build": "turbo run build",
		"publish-packages": "turbo run build && changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.28.1",
		"turbo": "^2.4.4"
	}
}
